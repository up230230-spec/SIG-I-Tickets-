/**
 * Módulo B — Mis tickets (usuario general): consultar estado de reportes propios.
 * STUB: listará tickets con <TicketCard /> (línea de orilla por tipo).
 */
export default function MyTickets() {
  return (
    <section className="page">
      <h1>Mis reportes</h1>
      <p className="meta">TODO: listar tickets propios con su estado actual.</p>
    </section>
  );
}
