/**
 * Módulo D — Panel global (Equipo de Operaciones).
 * STUB: vista global de tickets (refresco automático cada 60s), mapa de calor,
 * eliminación con doble confirmación para críticos y reportes PDF/CSV.
 */
export default function GlobalPanel() {
  return (
    <section className="page">
      <h1>Panel global de operaciones</h1>
      <p className="meta">TODO: vista global, mapa de calor y exportación de reportes.</p>
    </section>
  );
}
