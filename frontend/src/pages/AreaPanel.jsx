/**
 * Módulo C — Panel de gestión por área (Administrador de Área).
 * STUB: tickets del área, filtros por estado/severidad, código de colores,
 * cambio de estado y reasignación con nota de motivo.
 */
export default function AreaPanel() {
  return (
    <section className="page">
      <h1>Panel de área</h1>
      <p className="meta">TODO: tickets del área, filtros, flujo de estados y reasignación.</p>
    </section>
  );
}
